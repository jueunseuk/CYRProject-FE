import styled from "styled-components";

const ResetPassword = () => {
    return (
        <Wrapper>
            
        </Wrapper>
    );
}

export default ResetPassword;

const Wrapper = styled.div`
    
`;
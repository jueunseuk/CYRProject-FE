import styled from "styled-components";

const NaverCallback = () => {
    return (
        <Wrapper>
            
        </Wrapper>
    );
}

export default NaverCallback;

const Wrapper = styled.div`
    
`;